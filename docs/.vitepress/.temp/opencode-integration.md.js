import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"OpenCode Integration","description":"","frontmatter":{},"headers":[],"relativePath":"opencode-integration.md","filePath":"opencode-integration.md","lastUpdated":1776947908000}');
const _sfc_main = { name: "opencode-integration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="opencode-integration" tabindex="-1">OpenCode Integration <a class="header-anchor" href="#opencode-integration" aria-label="Permalink to &quot;OpenCode Integration&quot;">​</a></h1><p>LoopTroop uses OpenCode as its model execution layer, but wraps it with its own session ownership, context assembly, event streaming, and workflow recovery logic.</p><h2 id="core-modules" tabindex="-1">Core Modules <a class="header-anchor" href="#core-modules" aria-label="Permalink to &quot;Core Modules&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>server/opencode/adapter.ts</code></td><td>Concrete OpenCode SDK adapter and interface</td></tr><tr><td><code>server/opencode/factory.ts</code></td><td>Singleton adapter creation and mock-mode switching</td></tr><tr><td><code>server/opencode/sessionManager.ts</code></td><td>Session ownership, reconnect, completion, abandonment</td></tr><tr><td><code>server/opencode/contextBuilder.ts</code></td><td>Phase-specific context assembly</td></tr><tr><td><code>server/workflow/runOpenCodePrompt.ts</code></td><td>Prompt orchestration and stream handling</td></tr></tbody></table><h2 id="adapter-surface" tabindex="-1">Adapter Surface <a class="header-anchor" href="#adapter-surface" aria-label="Permalink to &quot;Adapter Surface&quot;">​</a></h2><p>The current <code>OpenCodeAdapter</code> interface exposes:</p><table tabindex="0"><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>createSession()</code></td><td>Create a new OpenCode session for a project path</td></tr><tr><td><code>promptSession()</code></td><td>Send prompt parts into an existing session</td></tr><tr><td><code>listSessions()</code></td><td>Enumerate remote sessions</td></tr><tr><td><code>getSessionMessages()</code></td><td>Read session message history</td></tr><tr><td><code>subscribeToEvents()</code></td><td>Stream OpenCode events</td></tr><tr><td><code>listPendingQuestions()</code></td><td>Read pending human-input requests</td></tr><tr><td><code>replyQuestion()</code></td><td>Answer a pending request</td></tr><tr><td><code>rejectQuestion()</code></td><td>Reject a pending request</td></tr><tr><td><code>abortSession()</code></td><td>Abort a remote session</td></tr><tr><td><code>assembleBeadContext()</code></td><td>Build bead-context prompt parts</td></tr><tr><td><code>assembleCouncilContext()</code></td><td>Build council prompt parts</td></tr><tr><td><code>checkHealth()</code></td><td>Health and availability check</td></tr></tbody></table><h2 id="base-url-and-modes" tabindex="-1">Base URL And Modes <a class="header-anchor" href="#base-url-and-modes" aria-label="Permalink to &quot;Base URL And Modes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Setting</th><th>Meaning</th></tr></thead><tbody><tr><td><code>LOOPTROOP_OPENCODE_BASE_URL</code></td><td>Base URL for the OpenCode server</td></tr><tr><td><code>LOOPTROOP_OPENCODE_MODE=mock</code></td><td>Use the mock adapter instead of the SDK adapter</td></tr></tbody></table><p>If no base URL is set, LoopTroop defaults to <code>http://127.0.0.1:4096</code>.</p><h2 id="session-ownership" tabindex="-1">Session Ownership <a class="header-anchor" href="#session-ownership" aria-label="Permalink to &quot;Session Ownership&quot;">​</a></h2><p>LoopTroop does not treat OpenCode sessions as anonymous chat handles. It tracks who owns a session in the project database.</p><p>Current ownership dimensions can include:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;ticketId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AUTH-12&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;phase&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;CODING&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;phaseAttempt&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;memberId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;beadId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;api-refresh-endpoint&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;iteration&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;step&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">null</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>This is what lets the backend distinguish:</p><ul><li>one council member&#39;s vote session from another</li><li>the first execution attempt for a bead from the second</li><li>a planning session from a coding session on the same ticket</li></ul><h2 id="prompt-runner" tabindex="-1">Prompt Runner <a class="header-anchor" href="#prompt-runner" aria-label="Permalink to &quot;Prompt Runner&quot;">​</a></h2><p><code>runOpenCodePrompt()</code> is the main orchestration helper.</p><p>It currently does the following:</p><ol><li>Resolve or create the session.</li><li>If <code>sessionOwnership</code> is present, call <code>SessionManager.validateAndReconnect()</code> first.</li><li>Dispatch the prompt with tool policy and model settings.</li><li>Subscribe to stream events while the prompt is running.</li><li>Reconcile the final response with assistant messages and stream status.</li><li>Mark the session completed or abandoned depending on the outcome.</li></ol><p><code>runOpenCodeSessionPrompt()</code> is the lower-level helper for prompting a known session.</p><h2 id="reconnect-behavior" tabindex="-1">Reconnect Behavior <a class="header-anchor" href="#reconnect-behavior" aria-label="Permalink to &quot;Reconnect Behavior&quot;">​</a></h2><p>Reconnect is intentionally conservative.</p><p><code>SessionManager.validateAndReconnect()</code> only succeeds when:</p><ul><li>the ticket still exists</li><li>the ticket is still in the same phase</li><li>the owned active session record still exists in the project DB</li><li>the same session still exists remotely in OpenCode</li></ul><p>If any of those checks fail, LoopTroop falls back to creating a fresh session.</p><p>That means LoopTroop can survive restart and resume safely, but it does not try to magically continue any random broken stream from the past.</p><h2 id="streaming" tabindex="-1">Streaming <a class="header-anchor" href="#streaming" aria-label="Permalink to &quot;Streaming&quot;">​</a></h2><p>OpenCode stream events are consumed server-side and then translated into LoopTroop&#39;s own ticket event model.</p><p>The prompt runner tracks:</p><ul><li>text events</li><li>reasoning events</li><li>tool events</li><li>step start and finish events</li><li>session status events</li><li>session error events</li></ul><p>The frontend never talks directly to OpenCode. It receives normalized ticket events over <code>/api/stream</code>.</p><h2 id="questions-and-human-input" tabindex="-1">Questions And Human Input <a class="header-anchor" href="#questions-and-human-input" aria-label="Permalink to &quot;Questions And Human Input&quot;">​</a></h2><p>OpenCode may request user input during execution. LoopTroop exposes that queue through:</p><ul><li><code>GET /api/tickets/:id/opencode/questions</code></li><li><code>POST /api/tickets/:id/opencode/questions/:requestId/reply</code></li><li><code>POST /api/tickets/:id/opencode/questions/:requestId/reject</code></li></ul><p>This lets the workflow remain durable even when the model pauses for an explicit decision.</p><h2 id="health-and-model-discovery" tabindex="-1">Health And Model Discovery <a class="header-anchor" href="#health-and-model-discovery" aria-label="Permalink to &quot;Health And Model Discovery&quot;">​</a></h2><p>LoopTroop uses two related but different checks:</p><table tabindex="0"><thead><tr><th>Check</th><th>Purpose</th></tr></thead><tbody><tr><td><code>adapter.checkHealth()</code></td><td>Basic OpenCode availability and version</td></tr><tr><td><code>/api/models</code></td><td>Provider catalog flattening and connected-model discovery</td></tr></tbody></table><p>If model discovery fails but health still passes, the API returns an empty model list plus a message instead of crashing the UI.</p><h2 id="startup-recovery" tabindex="-1">Startup Recovery <a class="header-anchor" href="#startup-recovery" aria-label="Permalink to &quot;Startup Recovery&quot;">​</a></h2><p>On startup, LoopTroop:</p><ul><li>checks OpenCode health</li><li>hydrates ticket actors from storage</li><li>scans active session records in attached project databases</li><li>attempts reconnect for owned sessions</li><li>abandons stale session records that no longer exist remotely</li></ul><p>This is why the OpenCode integration is part of the runtime architecture, not just a transport detail.</p><h2 id="why-looptroop-wraps-opencode-this-heavily" tabindex="-1">Why LoopTroop Wraps OpenCode This Heavily <a class="header-anchor" href="#why-looptroop-wraps-opencode-this-heavily" aria-label="Permalink to &quot;Why LoopTroop Wraps OpenCode This Heavily&quot;">​</a></h2><p>OpenCode is the model execution engine. LoopTroop adds:</p><ul><li>phase-aware context assembly</li><li>ticket-aware session ownership</li><li>durable restart behavior</li><li>workflow-aware retries</li><li>frontend-ready event projection</li></ul><p>Without that wrapper, the rest of the system would have no safe way to restart, audit, or recover a long-running ticket lifecycle.</p><h2 id="related-docs" tabindex="-1">Related Docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related Docs&quot;">​</a></h2><ul><li><a href="./context-isolation">Context Isolation</a></li><li><a href="./execution-loop">Execution Loop</a></li><li><a href="./api-reference">API Reference</a></li><li><a href="./system-architecture">System Architecture</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("opencode-integration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const opencodeIntegration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  opencodeIntegration as default
};
