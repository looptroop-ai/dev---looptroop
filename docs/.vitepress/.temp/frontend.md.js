import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Frontend","description":"","frontmatter":{},"headers":[],"relativePath":"frontend.md","filePath":"frontend.md","lastUpdated":1776947908000}');
const _sfc_main = { name: "frontend.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="frontend" tabindex="-1">Frontend <a class="header-anchor" href="#frontend" aria-label="Permalink to &quot;Frontend&quot;">​</a></h1><p>The frontend is a React 19 SPA that renders the ticket dashboard, the live workspace, review panes, and the navigator surfaces around them.</p><p>The UI is data-driven from:</p><ul><li><code>/api/*</code> REST endpoints</li><li><code>/api/stream</code> SSE updates</li><li>workflow metadata in <code>shared/workflowMeta.ts</code></li><li>ticket artifacts and runtime state from the backend</li></ul><h2 id="top-level-composition" tabindex="-1">Top-Level Composition <a class="header-anchor" href="#top-level-composition" aria-label="Permalink to &quot;Top-Level Composition&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Area</th><th>Purpose</th><th>Primary files</th></tr></thead><tbody><tr><td>App shell</td><td>App startup, configuration preload, global layout</td><td><code>src/App.tsx</code></td></tr><tr><td>Ticket dashboard</td><td>Ticket list, status cards, workspace selection</td><td><code>src/components/ticket/*</code></td></tr><tr><td>Active workspace</td><td>Chooses the live view for the selected phase</td><td><code>src/components/ticket/ActiveWorkspace.tsx</code></td></tr><tr><td>Navigator</td><td>Timeline, approval navigation, context tree, errors, full log entry point</td><td><code>src/components/ticket/NavigatorPanel.tsx</code></td></tr><tr><td>Workspace views</td><td>Draft, council, interview, approval, coding, error, canceled, review, full log</td><td><code>src/components/workspace/*</code></td></tr></tbody></table><h2 id="active-workspace-routing" tabindex="-1">Active Workspace Routing <a class="header-anchor" href="#active-workspace-routing" aria-label="Permalink to &quot;Active Workspace Routing&quot;">​</a></h2><p><code>ActiveWorkspace.tsx</code> maps workflow metadata to concrete views.</p><table tabindex="0"><thead><tr><th><code>uiView</code></th><th>Current component</th></tr></thead><tbody><tr><td><code>draft</code></td><td><code>DraftView</code></td></tr><tr><td><code>council</code></td><td><code>CouncilView</code></td></tr><tr><td><code>interview_qa</code></td><td><code>InterviewQAView</code></td></tr><tr><td><code>approval</code></td><td><code>ApprovalView</code></td></tr><tr><td><code>coding</code></td><td><code>CodingView</code></td></tr><tr><td><code>error</code></td><td><code>ErrorView</code></td></tr><tr><td><code>done</code></td><td><code>CodingView</code></td></tr><tr><td><code>canceled</code></td><td><code>CanceledView</code></td></tr></tbody></table><p>Additional routing rules:</p><ul><li>historical phases usually render through <code>PhaseReviewView</code></li><li><code>fullLogOpen</code> forces <code>FullLogView</code></li><li>reviewable past coding still uses <code>CodingView</code> in read-only mode</li><li>active or selected error occurrences render <code>ErrorView</code></li></ul><h2 id="navigator-surfaces" tabindex="-1">Navigator Surfaces <a class="header-anchor" href="#navigator-surfaces" aria-label="Permalink to &quot;Navigator Surfaces&quot;">​</a></h2><p><code>NavigatorPanel.tsx</code> is more than a left rail. It combines several different navigation modes:</p><ul><li><code>PhaseTimeline</code> for the workflow spine</li><li><code>ErrorOccurrencesPanel</code> for active and past failures</li><li><code>ApprovalNavigator</code> for interview, PRD, and beads approval context</li><li><code>ContextTree</code> for context visibility</li><li>a full-log toggle that opens <code>FullLogView</code></li></ul><p>That split matters because the workspace is designed for both live work and historical review.</p><h2 id="key-workspace-views" tabindex="-1">Key Workspace Views <a class="header-anchor" href="#key-workspace-views" aria-label="Permalink to &quot;Key Workspace Views&quot;">​</a></h2><table tabindex="0"><thead><tr><th>View</th><th>Primary purpose</th></tr></thead><tbody><tr><td><code>DraftView</code></td><td>Ticket editing and start controls</td></tr><tr><td><code>CouncilView</code></td><td>Multi-model draft and vote phases with artifacts</td></tr><tr><td><code>InterviewQAView</code></td><td>Interactive interview batches, draft persistence, skip flow</td></tr><tr><td><code>ApprovalView</code></td><td>Review and edit interview, PRD, beads, and execution setup artifacts</td></tr><tr><td><code>CodingView</code></td><td>Active bead execution, bead list, logs, diffs, verification actions</td></tr><tr><td><code>ErrorView</code></td><td>Live blocked state or historical error occurrence review</td></tr><tr><td><code>PhaseReviewView</code></td><td>Historical artifact review for completed phases</td></tr><tr><td><code>FullLogView</code></td><td>Full folded execution log stream</td></tr></tbody></table><h2 id="coding-workspace-surfaces" tabindex="-1">Coding Workspace Surfaces <a class="header-anchor" href="#coding-workspace-surfaces" aria-label="Permalink to &quot;Coding Workspace Surfaces&quot;">​</a></h2><p>The coding workspace is broader than a simple log pane.</p><p>Current <code>CodingView</code> composes:</p><ul><li>bead list and progress UI</li><li><code>BeadDiffViewer</code></li><li><code>CollapsiblePhaseLogSection</code></li><li><code>LogEntryRow</code></li><li><code>PhaseArtifactsPanel</code></li><li><code>VerificationSummaryPanel</code></li></ul><p>It also merges persisted bead artifacts with runtime bead overlays from the live ticket payload so the UI can show in-progress status and notes without waiting for a full artifact refresh.</p><h2 id="data-hooks" tabindex="-1">Data Hooks <a class="header-anchor" href="#data-hooks" aria-label="Permalink to &quot;Data Hooks&quot;">​</a></h2><h3 id="workflow-and-artifacts" tabindex="-1">Workflow And Artifacts <a class="header-anchor" href="#workflow-and-artifacts" aria-label="Permalink to &quot;Workflow And Artifacts&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Hook</th><th>Current role</th><th>Current return shape</th></tr></thead><tbody><tr><td><code>useWorkflowMeta()</code></td><td>Loads phase and group metadata</td><td><code>{ groups, phases, phaseMap, isLoading }</code></td></tr><tr><td><code>useTicketArtifacts(ticketId, opts?)</code></td><td>Fetches and caches ticket artifacts</td><td><code>{ artifacts, isLoading }</code></td></tr><tr><td><code>useTicketPhaseAttempts(ticketId?, phase?)</code></td><td>Reads phase-attempt history</td><td>React Query result</td></tr></tbody></table><h3 id="ticket-and-profile-data" tabindex="-1">Ticket And Profile Data <a class="header-anchor" href="#ticket-and-profile-data" aria-label="Permalink to &quot;Ticket And Profile Data&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Hook</th><th>Current role</th></tr></thead><tbody><tr><td><code>useTickets(projectId?)</code></td><td>Ticket list with auto-refresh for active tickets</td></tr><tr><td><code>useTicket(id)</code></td><td>Individual ticket with active-state refresh</td></tr><tr><td><code>useProfile()</code></td><td>Singleton profile query against <code>/api/profile</code></td></tr><tr><td><code>useOpenCodeModels()</code></td><td>Connected models only</td></tr><tr><td><code>useAllOpenCodeModels()</code></td><td>Full catalog including disconnected providers</td></tr></tbody></table><h3 id="live-updates" tabindex="-1">Live Updates <a class="header-anchor" href="#live-updates" aria-label="Permalink to &quot;Live Updates&quot;">​</a></h3><p><code>useSSE({ ticketId, onEvent })</code> is the ticket stream hook.</p><p>Current behavior:</p><ul><li>connects to <code>/api/stream</code></li><li>sends <code>ticketId</code> and <code>lastEventId</code> on reconnect</li><li>listens for <code>state_change</code>, <code>progress</code>, <code>log</code>, <code>error</code>, <code>bead_complete</code>, <code>needs_input</code>, and <code>artifact_change</code></li><li>invalidates or patches React Query caches in response</li><li>returns <code>{ lastEventIdRef, connectionState }</code></li></ul><p>Current <code>connectionState</code> values are:</p><ul><li><code>connecting</code></li><li><code>connected</code></li><li><code>reconnecting</code></li></ul><h2 id="interview-draft-persistence" tabindex="-1">Interview Draft Persistence <a class="header-anchor" href="#interview-draft-persistence" aria-label="Permalink to &quot;Interview Draft Persistence&quot;">​</a></h2><p><code>useBatchSubmit(ticketId)</code> is one of the higher-value stateful hooks in the app.</p><p>It does more than submit answers:</p><ul><li>stores draft answers per interview batch</li><li>tracks skipped questions</li><li>tracks selected options</li><li>restores drafts from persisted UI state</li><li>auto-saves drafts with debounce through ticket UI-state artifacts</li><li>coordinates submit and skip mutations</li><li>listens for interview batch updates coming back from the runtime</li></ul><p>That makes <code>InterviewQAView</code> resilient across reloads, view changes, and follow-up question rounds.</p><h2 id="artifact-and-review-surfaces" tabindex="-1">Artifact And Review Surfaces <a class="header-anchor" href="#artifact-and-review-surfaces" aria-label="Permalink to &quot;Artifact And Review Surfaces&quot;">​</a></h2><p>Several UI components exist specifically to inspect durable workflow state:</p><table tabindex="0"><thead><tr><th>Component</th><th>Purpose</th></tr></thead><tbody><tr><td><code>PhaseArtifactsPanel</code></td><td>Phase-specific artifact viewer</td></tr><tr><td><code>WorkspacePhaseSummary</code></td><td>Compact summary for the selected phase</td></tr><tr><td><code>VerificationSummaryPanel</code></td><td>Delivery actions during PR review</td></tr><tr><td><code>PhaseReviewView</code></td><td>Historical artifact review with phase-attempt support</td></tr><tr><td><code>FullLogView</code></td><td>Rawer, fuller log inspection</td></tr></tbody></table><p>The frontend is built around the assumption that users must be able to inspect prior attempts and artifacts without replaying the run mentally from logs.</p><h2 id="frontend-state-relationship-to-workflow-metadata" tabindex="-1">Frontend-State Relationship To Workflow Metadata <a class="header-anchor" href="#frontend-state-relationship-to-workflow-metadata" aria-label="Permalink to &quot;Frontend-State Relationship To Workflow Metadata&quot;">​</a></h2><p>The frontend does not hardcode the full workflow. Instead, it derives major behavior from <code>shared/workflowMeta.ts</code>:</p><ul><li>group ordering for the timeline</li><li>phase labels</li><li><code>uiView</code> mapping</li><li>whether a phase exposes a review artifact type</li><li>whether a phase is editable</li><li>whether multi-model logs are expected</li><li>whether a phase has question or bead progress semantics</li></ul><p>This is why keeping the docs aligned with <code>workflowMeta</code> matters: the UI is built around that shared metadata contract.</p><h2 id="related-docs" tabindex="-1">Related Docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related Docs&quot;">​</a></h2><ul><li><a href="./api-reference">API Reference</a></li><li><a href="./state-machine">State Machine</a></li><li><a href="./opencode-integration">OpenCode Integration</a></li><li><a href="./system-architecture">System Architecture</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("frontend.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const frontend = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  frontend as default
};
