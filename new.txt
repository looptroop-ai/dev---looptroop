Start the app. Opencode serve is running in another process if needed. For each request below check using agent-browser if you did an EXCELLENT work.



1. when i try to create a new project and select a folder that is really git initialised i get this error: "Browser selected "LoopTroop" but cannot expose the full path. Paste the absolute path manually." While pasting manualy the path works, i do not want to do this. browse should work, fix it to work for linux, wsl and macos.
6. I WANT THE WHOLE LOG IN LOG VIEW. use ref mcp and see how to implement the whole log from opencode. Persist log, save it per status.
13. Implement Interview Q&A batch system, read architecture.md spec how the flow should work. 
14. answering questions and then leaving the ticket and then returning to the ticket will loose all previous answers. check architecture.md how to save state so previous answers are not lost. this should happen across the whole app, not only during answering questions.
15. app is stuck in coverage check (interview), verify in architecture.md what it should really do in each coverage check statuses. Also coverage check status should be done with only winning ai council member, other ai models should not be present.
17. i am mostlly sure, as log does not work and i can't check, that correct context is not passed to ai models. check again architecture.md and see what context should opencode give to models. i think codebase map is not implemented correctlly or implemented at all.
18. voting on best drafts phase passes too quickly, i think it is not implemented correctlly, check again arhictecture.md for the required flow. Here beside the voting results, add the winner draft too as artifact.
19. check all the app and see if there are any more placeholders and if there are, implement the logic correctlly as per architecture.md.
23. Can you animate the icons for the actions when models are doing different actions (eg when models create a draft it shows drafting with a pencil icon - can this pencil be animated or a similar animation to be used).
24. voting results are surrely not done correctlly. check architecture.md and implement correctlly the flow, read what context should each model receive and make sure they do receive it via opencode.



Delete the current projects and tickets and create 3 new mock projects with data and 20 mock tickets in different statuses, but add mock data for previous statuses too, respecting the files requirments.
Before the very end create a ticket and make a full lifecycle checking each status with agent-browser to see if it works coreectly (including creating a project and a new ticket).
For ui changes use the skills available. If you need newer docuemntation use ref mcp server (especially for opencode).
architecture.md should be used in case some things are not fully understood.
do check, lints and anything needed to check you implemented correctlly.

At the very end check using curl and or agent-browser the app is working. wait 60 seconds and then check again and only then tell me that you finished.
Check if anything else needs to be added to .gitignore. Then commit and push everything, including other files not modified during this session.