// Action stubs — to be implemented in later milestones

export const persistState = () => {
  // Will persist XState snapshot to SQLite
}

export const persistBeadsTracker = () => {
  // Will persist bead progress to issues.jsonl
}

export const notifyFrontend = () => {
  // Will broadcast SSE event to connected clients
}

export const gitCommitAndPush = () => {
  // Will git add (allowlist) + commit + push
}

export const finalMergeToMain = () => {
  // Will merge ticket branch to main
}
